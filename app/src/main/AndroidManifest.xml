<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Not strictly required by AudioManager's Bluetooth-SCO methods on
         stock Android, but some OEM builds gate related audio-routing
         behind it - requested defensively so automatic Bluetooth mic
         routing has the best chance of working across devices. Wired/USB
         mic auto-routing doesn't need this at all. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Reading the user's own music library (API 33+ split media permissions) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Pre-API-33 fallback for reading files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Lets FolderBrowserActivity list/read any folder in shared storage
         directly, without any OS document-picker app being involved at
         all. Added specifically because some bare-bones/car-head-unit
         Android builds ship with no app that can handle
         ACTION_OPEN_DOCUMENT_TREE, which made "Add Music Folder" a dead
         end on those devices - this permission is what makes an
         in-app-drawn folder browser possible instead of depending on one.
         Google Play restricts this permission for store-listed apps, but
         this app is side-loaded (built via GitHub Actions, installed
         directly), so that restriction doesn't apply here. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CarVoicePlayer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".FolderBrowserActivity"
            android:exported="false"
            android:label="Choose a folder" />

        <service
            android:name=".VoiceService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
